services:
  sql-server-host:
    container_name: sqlServerHost2025
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "1433:1433"
    volumes:
      - siteHostTest:/var/opt/mssql
    tty: true
volumes:
  siteHostTest:
