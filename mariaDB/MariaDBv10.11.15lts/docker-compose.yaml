services:
  marias-db-host:
    container_name: mariasDb2026
    build: 
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "3308:3306"
    volumes:
      - siteHostTest:/var/lib/mysql
    tty: true
volumes:
  siteHostTest: